version: '3.7'

services:
  holidays-backend:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./src:/myfolder/src
    ports:
      - 3000:3000
    env_file:
      - ./.env.docker
  holidays-redis:
    image: redis:latest
    ports:
      - 6379:6379
